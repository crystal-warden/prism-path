__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
# runtime artifacts (never commit)
*.bak-*
interactions.jsonl
mission_audit.log
mc_sprint.log
*.jsonl.tmp

# Area-5 comparison venv (recreate from prismpath/comparisons/requirements.txt)
prismpath/comparisons/.venv/

# local test venv (recreate with pip install -e .[test])
.venv/

# Cargo build output — 333 object files were tracked before this line existed,
# turning every build into hundreds of phantom git modifications
prismpath-rs/target/
